# Add these three lines ON IRAN ONLY to an existing validated config.
# No multi-domain or HTTP/2. Do not add edge_dns_rotation=1.
tls_backend=utls
tls_adapter_socket=/run/pacetun-utls/bridge.sock
edge_dns_rotation=0

pool_drain_sec=30
pool_idle_poll_ms=100
address_family=auto
