# Merge these keys into the existing core config
traffic_shaping=1
websocket_padding_max_bytes=256
websocket_control_padding_bytes=32
padding_budget_pct=5
shaping_startup_ms=1500
shaping_delay_ms=3
shaping_rate_bytes=4096
shaping_burst_bytes=4096
# Iran only: match the selected uTLS bridge template; optional, not a real browser.
browser_profile=firefox120
